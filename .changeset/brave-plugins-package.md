---
"@lenso/cli": minor
---

Add the unified `lenso plugin new`, `dev`, `check`, and `pack` workflow, move
built-in authoring under `lenso module`, and hide the deprecated dual-path
commands for one compatibility window.
