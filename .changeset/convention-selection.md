---
"@lenso/cli": minor
---

Add extensible local file conventions and bundled CLI support with Rust and TypeScript scaffolds, selected-only optional dependencies, read-only diagnostics, and source-free command distributions. TypeScript CLI Apps require Bun for authoring but no Rust toolchain.
