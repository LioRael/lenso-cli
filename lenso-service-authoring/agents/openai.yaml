interface:
  display_name: "Lenso Service Authoring"
  short_description: "Build an out-of-process service that provides Lenso Modules."
  default_prompt: "Use $lenso-service-authoring to build or change this Lenso service through its public manifest and host-managed lifecycle."
